// Linked List Implementation. There are two types of Linked List. 1. Single Linked List . 2, Double Linked List
// Here are implementation especailly single linked list data structure.
// Feature of Linked List. Node ==> ( Value, next ( next means next node address)). Insert Node, remove node . start from Head and End from tel

// Single LinkedList Emplementation // initial node 

class Node{
    constructor(element){
        this.element = element;
        this.next = null
    }
}

// create another class for maintain nessecery function, Head & Size ( Head means where to start insert node )

class linkedList{
    constructor(){
        this.head = null;
        this.size = 0
    }

    // here add all the nessecery function 
    
    // for add element I mean new node 
    add(element){

    }

    insertAtValue( element, index){

    }

    removeForm(location){

    }

    removeElement(element){

    }

    isEmpty(){

    }

    size_of_list(){

    }

    printList(){

    }

}

